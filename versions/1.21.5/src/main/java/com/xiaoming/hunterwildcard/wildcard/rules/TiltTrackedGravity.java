package com.xiaoming.hunterwildcard.wildcard.rules;

import net.minecraft.util.math.Direction;

public interface TiltTrackedGravity {
    Direction hunterwildcard$gravity();
    void hunterwildcard$gravity(Direction direction);
}
