# 更新日志

[English](CHANGELOG.en.md) | 简体中文

## 1.4.0

### 修复

- 后室：一方全员离开后室时服务端崩溃（整合服直接关闭）。原因是外卡在自己的 tick 里同步停止自己，把当前外卡置空后管理器继续访问它；现在停止请求延迟到本 tick 结束后执行。
- 死亡不掉落：模组现在在对局中接管原版 `keepInventory`（开局记录原值、强制关闭并每秒校验、结束后恢复），房主手动改原版规则不再让按阵营的掉落设置失效。
- M 键菜单：输入框内容为空或不合法时不再卡住翻页、滚动和关闭，改为回退到上一次有效值并提示；滚动和服务器同步不再丢失正在输入的内容和焦点；ESC 总能关闭。

### 外卡重做

- 新增 12 张外卡：回头杀、吸血鬼、闪电侠、移形换影、受伤瞬移、空间波动、传送门、静止发光、潜行冻结、地动山摇、掉落物炸弹、连锁挖矿。
- 删除纯 buff 卡：疾速追猎、轻盈之身、全员发光、暗夜追猎、指南针干扰、暂时停用。
- 重做：补给空投改为落在猎人与逃亡者中点、每名逃亡者一只箱子、落地前 20 秒广播坐标；猎人雷达改为逃亡者全程发光 + 猎人接近警告（警告距离可配）；血怒时刻改为血量不到 3 颗心时一击必杀；饥饿追逐简化为进食获得 10 秒速度 VI；武器过热的热量条放大并加了文字标签。
- 后室：每 20 秒双方同时发光 5 秒，猎人红、逃亡者蓝（所有发光效果都按阵营着色）。
- 开局前选队时按阵营发光，一眼看出分组。
- 逃亡者复活后，击杀他的猎人发光 10 秒并提示名字，复活后知道往哪跑。
- 击杀反馈卡恢复原来的大小；抽卡动画播放时隐藏对局状态卡。
- 后室环境音音量减半。
- 击杀判定新增「环境死亡计入击杀」开关（规则 › 击杀判定），关闭后纯环境死亡不再折算成猎人击杀。
- 掉落物炸弹：引信中的物品不再和旁边的掉落物合并，修复几件物品落在一起时有的不爆炸直接消失。
- 地动山摇：重力真正转向（关闭竖直重力、横向拉力），鼠标视角、移动、跳跃、贴墙站立和玩家模型全部按新的重力坐标系工作，切换有 3 秒动画；碰撞箱仍是竖直的，这是原版限制。
- 外卡间隔和持续时长各自支持「固定」或「随机（最短～最长）」；本局抽过的外卡权重按 1/(1+次数) 衰减，新外卡更容易被抽到。
- 菜单：外卡开关按「战斗 / 机动 / 视野 / 环境与道具」分类成卡片，每类可一键全开全关；首页把选队和开始按钮放到最上面；滚动不再重建控件（修复偶尔点不动输入框和下拉框），服务器同步在下拉框打开时延后；从外卡设置返回时回到原来的滚动位置；外卡设置页标题不再显示格式错误。
- 配置文件：外卡开关改为 `enabledWildcards` 表（旧的 `enableXxx` 会自动迁移），雷达间隔改为 `hunterRadarWarningDistance`，新增 `spaceShiftIntervalSeconds` 与时长/间隔模式字段。

### 玩法调整

- 追踪指南针：潜行 + 右键在「最近的逃亡者 → 逃亡者 A → 逃亡者 B → …」之间循环，右键打开选择菜单；物品名实时显示当前目标和距离（例如「追踪指南针 → 小明（137 格）」）。
- 击杀判定重做：逃亡者在被猎人打过后 15 秒内（可配置）的任何死亡都计为该猎人的击杀；纯环境死亡（摔死、怪物、自杀）单独计数，每满 N 次（默认 1，可配置）算一次猎人击杀，堵住靠自杀免费传送的漏洞。击杀反馈里环境击杀显示为「环境」。所有逃亡者死亡都有右上角反馈卡（含环境死亡进度）；击杀卡放大、带红色闪光和更重的音效。
- 死亡等待：等待复活期间全屏黑屏，只显示倒计时、死亡原因和剩余生命，人物固定不动，不能再用观察者视角侦查。
- 复活点：默认在死亡点 X～2X 格外的随机地表位置复活（逃亡者 150 格起且尽量远离所有猎人 150 格以上，猎人 50 格起），猎人不再靠死亡免费回出生点；可关闭，距离可配置。猎人复活点还会距所有逃亡者至少 200 格（可配），并且每死一次复活多等 5 秒（可配，上限 60 秒），避免复活后立刻缠住逃亡者。
- 存活指定时长模式：以出生点为中心加一个方形世界边界（半径默认 500 格，可配置、可关闭），逃亡者不能靠无限拉扯稳赢。
- 未加入阵营的玩家在对局中自动进入旁观模式，对局结束恢复。
- 部分规则可在对局中修改（移速、伤害倍率、胜利目标数值、外卡开关与参数、复活时间与复活点、击杀判定、掉落等），保存后全服提示「规则已更新」。
- 后室：取消大片出口区域，改为极少量散落的 4×4 假地板格（约每 500 格一个），只出现在通道格内，不会贴在墙下。

### 界面与 HUD

- 状态 HUD 精简：等待面板去掉猎人复活和准备时间行，对局中只显示阶段、身份和当前外卡；身份 ActionBar 只在开局前 15 秒提示。
- 猎人名字红色、逃亡者蓝色（TAB、聊天、头顶名牌、追踪条），通过记分板队伍实现，对局结束后清除。
- 原版追踪条对局中保持开启，但只显示自己阵营的玩家（可关闭）。
- 左侧目标面板新增猎人进度行：击杀计数模式显示「猎人击杀 3/10」，出局模式显示「逃亡者存活 2/3 · 总命数 4」。
- M 键菜单精简为「对局 / 规则 / 外卡」三页（调试页保留），规则页下再分「时间与边界 / 胜利条件 / 复活与死亡 / 击杀判定 / 阵营平衡」子页；队伍加入合并到对局页，对局状态面板改为对局页里的按钮切换；结算时间、指南针刷新、ActionBar 间隔、边界警告距离不再出现在界面里（配置文件仍可改）。

## 1.3.1

- 修复后室环境音不播放：循环从音量 0 淡入，而声音引擎会直接跳过静音的声音，现在标记为始终播放；声音事件改为在主初始化阶段注册，进入维度 1 秒后再启动并每秒自检重启，音量从 0.08 提高到 0.2。客户端测试新增“环境音正在播放”断言。

## 1.3.0

### 新增外卡

- 受伤乱键：玩家每次受伤都会随机打乱前进、后退、左右、跳跃、潜行、疾跑键位，右上角新增 HUD 实时显示当前键位；外卡结束或断线时自动恢复原始键位。
- 全体变小：所有参与者缩小到一半体型（约一格高），视角与碰撞箱同步缩小。
- 小心翼翼：所有参与者生命上限变为 3 颗心，外卡结束后按比例恢复。
- 你是谁？：全员史蒂夫皮肤，隐藏头顶名牌，TAB 列表和聊天中的名字都显示为「玩家」。
- 别过来！：逃亡者获得锋利 255 的无限耐久金剑，无法丢弃，猎人无法拾取，结束后自动回收。
- 后室！：新增 `hunterwildcard:backrooms` 维度（移植自 The Fourth Frequency 的未渲染层，含贴图与环境音）。触发后全员相机下沉 + 黑屏，落入迷宫中彼此错开的位置；猎人每 20 秒发红光 1 秒，逃亡者靠它躲，猎人靠指南针追；迷宫无怪物、无法破坏，放置的方块 10 秒后消失；踩到假地板掉出去即从主世界进入点上空无伤落回（洞穴 / 下界 / 末地则原地返回）；任一方全员离开或时间到则全体返回；后室内死亡保留物品栏；掉线重连自动送回；视距锁 6 区块，环境音低音量；持续时间单独配置，默认 240 秒。

### 新增设置

- 猪灵交易：可开关并设置金锭交易给出末影珍珠的总概率，默认开启且为 40%（原版约 2%）。设置位于配置界面「基础」页。
- 阵营平衡：猎人对逃亡者的伤害倍率、猎人移速倍率、逃亡者移速倍率（均以 100% 为原版）。

### 界面与 HUD

- 默认打开菜单的按键从 `H` 改为 `M`。
- 等待阶段自动在左上角显示「本局设置」HUD，让所有玩家看到当前胜利条件、复活规则和外卡设置。
- 对局开始后，非 OP 玩家按 `M` 可切换「对局状态」面板；服务器每秒同步一次状态。
- 修复菜单卡片显示不全：状态块过窄时自动换行而不是截断；窄卡片里标签放不下时自动换到控件上方一行；滚动到一半的外卡格子现在会完整绘制图标和名字。
- README 改为图文版，截图由客户端自动化测试（`./gradlew runClientGameTest`）生成。
- 「你是谁？」生效期间，猎人雷达、击杀 / 复活反馈、死亡消息中的玩家名也统一显示为「玩家」。

## 1.2.0 - Modrinth 首发版本

这是 Manhunt Wildcard 在 Modrinth 发布的第一个版本。本版本不是从 Modrinth 旧版本升级而来，而是作为公开首发版本提供完整的基础玩法体验。

### 首发内容

- 加入猎人 vs 逃亡者的 Manhunt 对局框架。
- 支持管理员使用 `/hw` 命令控制对局开始、停止、队伍状态和外卡测试。
- 加入准备阶段，减少开局混战，让玩家有时间进入各自阵营和位置。
- 为猎人提供追踪指南针，并按服务器配置周期刷新追踪目标。
- 周期性触发随机外卡事件，并通过 HUD、BossBar 和聊天消息提示当前规则变化。
- 支持多种逃亡者与猎人胜利条件。
- 支持可配置的复活模式、生命数、死亡掉落、准备边界、外卡间隔和外卡持续时间。
- 提供游戏内配置界面，OP 可在等待阶段调整主要规则。
- 提供中英文语言文件，支持 Minecraft 原生语言切换。

### 外卡内容

本版本包含 16 种外卡：

- 疾速追猎
- 轻盈之身
- 全员发光
- 暗夜追猎
- 死亡爆炸
- 补给空投
- 猎人雷达
- 指南针干扰
- 饥饿追逐
- 武器过热
- 轻装上阵
- 方块腐化
- 珍珠狂潮
- 风弹乱斗
- 血怒时刻
- 暂时停用

### 兼容性

- Minecraft `1.21.11`
- Fabric Loader `0.16.0+`
- Fabric API
- Java `21`

建议客户端和服务端同时安装本模组。服务端负责对局逻辑，客户端负责配置界面、HUD 和本地语言显示。
